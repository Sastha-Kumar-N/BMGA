import type { Metadata } from 'next';
import PolicyPage, { PolicySection } from '../components/PolicyPage';

export const metadata: Metadata = { title: 'Cookie Notice | BMGA', description: 'Cookie use in the Bharat Microbial Genome Atlas portal.' };
export default function CookiesPage() { return <PolicyPage eyebrow="Safety & Transparency" title="Cookie Notice" summary="This implementation uses only first-party cookies that are necessary for account sessions, security controls, and remembering that the cookie notice was acknowledged."><PolicySection title="Cookies in use"><p><strong>NextAuth session cookies:</strong> authenticate signed-in users and protect account or admin routes. Their exact names vary between HTTP development and secure HTTPS production.</p><p><strong>bmga_cookie_notice:</strong> records that this notice was acknowledged for up to 180 days. It contains no biological data, account details, or tracking identifier.</p></PolicySection><PolicySection title="No optional tracking"><p>No advertising, behavioral profiling, or analytics cookie is enabled by the current portal code. A future optional service must be added behind explicit consent and documented here before activation.</p></PolicySection><PolicySection title="Browser controls"><p>You can remove cookies through your browser settings. Removing the session cookie signs you out; removing the notice cookie causes the notice to appear again.</p></PolicySection></PolicyPage>; }

