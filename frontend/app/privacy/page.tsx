import type { Metadata } from 'next';
import PolicyPage, { PolicySection } from '../components/PolicyPage';

export const metadata: Metadata = { title: 'Privacy & Data Use | BMGA', description: 'Privacy, security, and biological data-use boundaries for BMGA.' };
export default function PrivacyPage() { return <PolicyPage eyebrow="Privacy & Responsible Use" title="Privacy and Data Use" summary="BMGA separates public reviewed genomic records from private account, submission, and administrative data. This page describes the controls implemented by the portal; it is not a substitute for an organization-approved legal privacy notice."><PolicySection title="Account and contact data"><p>Names, email addresses, affiliations, contact messages, submissions, and reviewer communications are stored in the backend database. Passwords are stored only as password hashes. Admin-only APIs enforce server-side role checks.</p></PolicySection><PolicySection title="Genomic submissions"><p>User-submitted organism metadata, FASTA, GFF3, and MAYA files remain private during review. Approval publishes the selected organism metadata and approved reference assets. Submitters should not upload human-identifiable information, credentials, protected clinical records, or data they are not authorized to share.</p></PolicySection><PolicySection title="BLAST queries"><p>BLAST queries are validated, processed with NCBI BLAST+ against approved BMGA references, and are not intentionally persisted by the application. Query sequence content is excluded from audit logs. Operational infrastructure may still record request metadata such as timestamp, account, IP address, result status, and request ID.</p></PolicySection><PolicySection title="Evidence limitations"><p>MAYA, genome browser, and BLAST outputs are computational and normally genotypic. They do not establish phenotype, pathogenicity, transmission, diagnosis, or treatment. Phenotypic evidence is labelled separately only when reported.</p></PolicySection><PolicySection title="Production governance"><p>Before public production use, the database owner must approve retention periods, incident response, data-subject request handling, jurisdiction-specific notices, data-processing agreements, and the dataset reuse license.</p></PolicySection></PolicyPage>; }

